description: |
  A reference to a Secret. It has enough information to retrieve Secret in any namespace.
x-kab-description-zh: |
  对某 Secret 的引用。其中包含的信息可供引用任何名字空间中 Secret。
properties:
  name:
    description: Name of the referenced Secret resource.
    x-kab-description-zh: 被引用的 Secret 资源的名称。
    minLength: 1
    type: string
  namespace:
    description: 被引用的 Secret 所在的名字空间名称。
    type: string
required:
  - name
type: object
x-kubernetes-map-type: atomic

