{
  "description": "TokenRequest contains parameters of a service account token.",
  "properties": {
    "audience": {
      "description": "The intended audience of the token in `TokenRequestSpec`. It will default to the audiences of kube apiserver.",
      "type": "string"
    },
    "expirationSeconds": {
      "description": "The duration of validity of the token in `TokenRequestSpec`. It has the same default value of `expirationSeconds` in `TokenRequestSpec`.",
      "format": "int64",
      "type": "integer"
    }
  },
  "required": [
    "audience"
  ],
  "type": "object"
}
