description: The current value for a metric
properties:
  averageUtilization:
    description: The current value of the average of the resource metric across all
      relevant Pods, represented as a percentage of the requested value of the resource
      for the Pods.
    format: int32
    type: integer
  averageValue:
    $ref: '#/definitions/io.k8s.apimachinery.pkg.api.resource.Quantity'
    description: The current value of the average of the metric across all relevant
      Pods (as a quantity)
  value:
    $ref: '#/definitions/io.k8s.apimachinery.pkg.api.resource.Quantity'
    description: The current value of the metric (as a quantity).
type: object

