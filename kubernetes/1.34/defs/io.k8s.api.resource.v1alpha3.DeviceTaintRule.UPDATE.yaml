description: |
  DeviceTaintRule adds one taint to all devices which match the selector.
  This has the same effect as if the taint was specified directly in the ResourceSlice by the DRA driver.
x-kab-description-zh: |
  DeviceTaintRule 向与选择算符匹配的所有设备添加一项污点。
  效果上等同于 DRA 驱动直接在 ResourceSlice 中为设备设置污点。
properties:
  apiVersion:
    description: The version of the schema for the object representation.
    x-kab-description-zh: 对象表示模式的版本。
    enum:
      - resource.k8s.io/v1alpha3
    type: string
  kind:
    description: A string value representing the REST resource.
    x-kab-description-zh: 代表 REST 资源的字符串值。
    enum:
      - DeviceTaintRule
    type: string
  metadata:
    $ref: '#/definitions/io.k8s.apimachinery.pkg.apis.meta.v1.ObjectMeta.UPDATE'
    description: Standard object metadata. This name must be a valid DNS subdomain name.
    x-kab-description-zh: 标准的对象元数据。`name` 必须是合法的 DNS 子域名。
  spec:
    $ref: '#/definitions/io.k8s.api.resource.v1alpha3.DeviceTaintRuleSpec'
    description: |-
      `spec` specifies the selector and one taint.

      Changing the `spec` automatically increments the `metadata.generation` number.
    x-kab-description-zh: |-
      `spec` 给出选择算符和污点定义。

      变更 `spec` 时，`metadata.generation` 中的数字会自动增一。
required:
  - apiVersion
  - kind
  - metadata
  - spec
type: object
x-kubernetes-group-version-kind:
  - group: resource.k8s.io
    kind: DeviceTaintRule
    version: v1alpha3

