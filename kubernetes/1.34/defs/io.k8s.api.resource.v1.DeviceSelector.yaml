description: DeviceSelector must have exactly one field set.
x-kab-description-zh: DeviceSelector 中必须且只能设置一个字段。
properties:
  cel:
    $ref: '#/definitions/io.k8s.api.resource.v1.CELDeviceSelector'
    description: This contains a CEL expression for selecting a device.
    x-kab-description-zh: 此字段包含一个用来选择设备的 CEL 表达式。
type: object

