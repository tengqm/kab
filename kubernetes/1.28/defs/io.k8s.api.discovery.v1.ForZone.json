{
  "description": "Information about which zones should consume this Endpoint.",
  "properties": {
    "name": {
      "description": "The name of the zone. It must be a valid label value, i.e. it must be an empty string or consist of alphanumeric characters, '-', '_', or '.', and must star and end with an alphanumeric character.",
      "maxLength": 63,
      "minLength": 0,
      "type": "string"
    }
  },
  "required": [
    "name"
  ],
  "type": "object"
}
