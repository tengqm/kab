{
  "description": "ResourceClaim references one entry in `pod.spec.resourceClaims`.",
  "properties": {
    "name": {
      "description": "Name must match the name of one entry in `spec.resourceClaims` of a Pod where this field is used. It makes that resource available inside a container.",
      "minLength": 1,
      "type": "string"
    }
  },
  "required": [
    "name"
  ],
  "type": "object"
}
