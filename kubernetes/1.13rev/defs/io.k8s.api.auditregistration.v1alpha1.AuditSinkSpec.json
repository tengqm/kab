{
  "description": "AuditSinkSpec holds the spec for the audit sink",
  "properties": {
    "policy": {
      "$ref": "#/definitions/io.k8s.api.auditregistration.v1alpha1.Policy",
      "description": "Policy defines the policy for selecting which events should be sent to the webhook"
    },
    "webhook": {
      "$ref": "#/definitions/io.k8s.api.auditregistration.v1alpha1.Webhook",
      "description": "Webhook to send events"
    }
  },
  "required": [
    "policy",
    "webhook"
  ],
  "type": "object"
}
