description: Information about the priority level used.
x-kab-description-zh: 关于所使用的优先级别的信息。
properties:
  name:
    description: |
      The name of a `PriorityLevelConfiguration` resource. The name must be a valid DNS subdomain name.
    x-kab-description-zh: |
      某个 `PriorityLevelConfiguration` 资源的名字。此字段必须是一个合法的 DNS 子域名。
    type: string
required:
  - name
type: object

