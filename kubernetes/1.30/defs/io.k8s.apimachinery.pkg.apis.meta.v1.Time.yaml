description: |
  Time is a wrapper around GoLang `time.Time` which supports correct marshaling to YAML and JSON.
x-kab-description-zh: |
  Time 是对 GoLang `time.Time` 的封装，支持封包转换为 YAML 和 JSON。
format: date-time
type: string

